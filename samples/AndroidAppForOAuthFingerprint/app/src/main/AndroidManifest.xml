<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibm.security.demoapps.oauthdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-permission android:name="android.permission.USE_FINGERPRINT" />
        <uses-permission android:name="android.permission.INTERNET" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PINAuthnActivity" />
        <activity android:name=".FingerprintAuthnActivity" />
        <activity android:name=".ShowbalanceActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".ChangepinActivity" />
        <activity android:name=".LogoutActivity" />
        <activity android:name=".ConfigurationActivity" />
        <activity android:name=".FingerprintEnrolmentActivity" />
        <activity android:name=".FingerprintUnenrolActivity" />
        <activity android:name=".WebLaunchActivity"></activity>
    </application>

</manifest>